import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const UserListItem = ({ user , handleFunction} :any) => {
  return (
    <View>
      <Text>UserListItem</Text>
    </View>
  )
}

export default UserListItem

const styles = StyleSheet.create({})